--
--  Copyright (c) 2024-2026, German Rivera
--
--  SPDX-License-Identifier: Apache-2.0
--
--  FRDM-KL25Z Autonomous Car - Zephyr Ada port
--  Target: NXP KL25Z (ARM Cortex-M0+, 16 KB SRAM, 128 KB flash)
--  Serial_Console excluded to save RAM; Runtime_Logs used for debug output.
--
with "config/frdm_kl25z_autonomous_car_config.gpr";
--  Both compiler_flags.gpr and zephyr_ada_rts.gpr are resolved by Alire
--  via GPR_PROJECT_PATH from the pinned zephyr_ada_rts crate directory.
with "compiler_flags.gpr";
with "zephyr_ada_rts.gpr";

project Frdm_Kl25z_Autonomous_Car is
   for Target use Compiler_Flags.Target_Compiler_Selector;
   for Runtime ("Ada") use Zephyr_Ada_Rts'Runtime ("Ada");
   for Languages use ("Ada");

   for Library_Name use "ada_app";
   for Library_Version use Project'Library_Name & ".a." & Frdm_Kl25z_Autonomous_Car_Config.Crate_Version;

   for Source_Dirs use (
      "src/",
      "src/building_blocks/portable/",
      "src/building_blocks/cpu_specific/arm_cortex_m0plus/",
      "src/building_blocks/board_specific/frdm_kl25z/",
      "src/building_blocks/mcu_specific/nxp_kinetis_kl25z/",
      "src/SVD/nxp_kinetis_kl25z/",
      "src/drivers/portable/",
      "src/drivers/mcu_specific/nxp_kinetis_kl25z/",
      "src/drivers/board_specific/frdm_kl25z/",
      "config/"
   );
   for Object_Dir use "obj/" & Frdm_Kl25z_Autonomous_Car_Config.Build_Profile & "/" & Compiler_Flags.Board;
   for Create_Missing_Dirs use "True";
   for Library_Dir use "lib/" & Compiler_Flags.Board;

   type Library_Type_Type is ("relocatable", "static", "static-pic");
   Library_Type : Library_Type_Type :=
     external ("FRDM_KL25Z_AUTONOMOUS_CAR_LIBRARY_TYPE", external ("LIBRARY_TYPE", "static"));
   for Library_Kind use Library_Type;

   for Library_Standalone use "standard";

   for Library_Interface use ("Ada_Main");

   package Compiler is
      for Default_Switches ("Ada") use Frdm_Kl25z_Autonomous_Car_Config.Ada_Compiler_Switches &
                                       Compiler_Flags.Compiler.ADAFLAGS;
   end Compiler;

  --  package Binder is
  --     for Switches ("Ada") use ("-Es"); --  Symbolic traceback
  --  end Binder;

end Frdm_Kl25z_Autonomous_Car;
